- Worked in using [qwen3-coder](https://ollama.com/library/qwen3-coder) directly from VSCode. Many tutorials indicated that [vscode-ollama](https://github.com/warm3snow/vscode-ollama) or [continue](https://github.com/continuedev/continue), but in fact direct integration to Copilot chat is possible, just click the configurations. Also worked on thermocline model with SU2 and wrote a note for dimensioning a generic system.
- Added the following to ensure an extension reload warning is not shown in Quarto (then later did some more robust and automated stuff in `00-autoreload.py`):
  
  ```python
  try:
      ip = get_ipython()
      if "majordome.magic" not in ip.extension_manager.loaded:
          ip.run_line_magic('load_ext', 'majordome.magic')
  except:
      pass
  ```
-